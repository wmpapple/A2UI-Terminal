# V2.x M0 执行与验证记录

## M0-LOG-001 — START

- 日期：2026-09-21（Asia/Shanghai）。
- 用户授权：开始实施；每阶段结束后必须等待用户人工检验，不得直接开始下一阶段。
- 本轮范围：仅 M0，现有代码基线核对、自动验证、桌面启动冒烟、ADR/接口草案、验收样本和人工验收文档。
- 基准：`35023d75a0d87e5e16f1d0de8a554e4eb6b49a69`（框架优化2）。
- 开始前工作树：已跟踪文件干净；`docs/V2.X/` 是此前两份未跟踪规划文档，保留其内容与用户安排的位置。
- 当前状态：进行中。F0、M1–M10 均不启动。
- 数据边界：使用仓库虚构 fixture 和临时数据库；不读取用户真实数据库/凭据，不向真实 Provider 发送内容，不操作安装器或发布。
- 验证计划：Node 22 下前端 check/coverage/E2E；Rust fmt/clippy/test；合同/权限/迁移快照；本地审计；debug 无安装包构建和隔离数据目录启动冒烟。
- 日志：`logs/v2x-m0/`（Git 忽略；验收文档保存结果和必要摘要）。
- 决策范围：工程接口先形成草案，不增加生产接口或迁移；AI 接入发布策略保持待选择，使用现有 Provider 合同进行本地确定性验证不等于选定 BYOK 发布。

## M0-LOG-002 — 基线缺陷与修复

- 自动检查：前端 check/coverage、Rust fmt/clippy/test、三项本地审计及 npm 生产依赖审计通过。
- 两次全量 E2E 分别 22/23、21/23；失败均为 page.goto 的 Page crashed。第二次失败项定向 2/2 通过；第一轮失败项第二轮已通过。保留失败 trace/video，不改断言或隐藏失败，根因未定。
- 发现旧冒烟脚本 APPDATA/LOCALAPPDATA 隔离不适用于当前 Tauri Known Folder 路径，尚未运行旧脚本即暂停桌面启动。
- M0 范围内修复冒烟隔离：新增显式临时根模式，SQLite/成果/WebView 隔离、系统凭据禁用、启动回执和旧二进制拒绝检查。普通启动路径保持原行为。
- 修复后 Rust 234 通过、1 个手工 PDF 预览生成器忽略；clippy 通过。无业务 API/权限/迁移/依赖变化。
- debug 无安装包构建和修复后的隔离冒烟通过；测试进程结束、临时目录清理。未触及用户数据库、密钥、远端 Provider 或安装器。

## M0-LOG-003 — 交付并等待人工验收

- 交付：[基线报告](V2_BASELINE.md)、[原始快照](M0_BASELINE_SNAPSHOT.json)、[交付快照](M0_DELIVERY_SNAPSHOT.json)、[ADR/接口草案](M0_ARCHITECTURE_DECISIONS.md)、[固定样本](M0_ACCEPTANCE_SAMPLES.md)、[人工验收清单](M0_MANUAL_ACCEPTANCE.md)。
- 状态：**待人工验收**，不是已完成验收。E2E 稳定性、真实 Provider/Windows 手工链路和发布依赖保留实际状态。
- AI 商业策略未选择，负责人按角色列出；草案未转成生产业务接口。
- 停止点：M0。本轮没有开始 F0、M1 或之后阶段；待用户人工检验结论，有问题先在 M0 修正。

## M0-LOG-004 — 用户人工验收通过

- 用户明确回复：“测试通过，开始下一阶段”。
- M0 状态变更为已完成人工验收；该回复授权 F0，不授权跳过后续阶段的人工验收。
- 前述自动验证结果和缺陷记录保留原貌；后续交付产物会覆盖 debug 输出位置，历史二进制指纹不代表当前构建。
