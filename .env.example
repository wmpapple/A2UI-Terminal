# API keys are intentionally not loaded from this file in the production app.
# Stage M0-C will store secrets in Windows Credential Manager.
# This file documents optional, non-secret development defaults only.

VITE_APP_LOCALE=zh-CN
